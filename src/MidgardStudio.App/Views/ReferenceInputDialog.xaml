<ui:FluentWindow x:Class="MidgardStudio.App.Views.ReferenceInputDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Choose"
                 Width="380" Height="440"
                 WindowStartupLocation="CenterOwner"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 ResizeMode="NoResize"
                 ShowInTaskbar="False">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar x:Name="TitleBarCtl" Grid.Row="0" Title="Choose" />
        <Grid Grid.Row="1" Margin="16,8,16,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="PromptText" Text="Mob" Opacity="0.85" Margin="0,0,0,4" />
            <ui:TextBox Grid.Row="1" x:Name="NameBox" PlaceholderText="Type to search…" />
            <Border Grid.Row="2" Margin="0,8,0,0" CornerRadius="6" Background="#16FFFFFF" BorderBrush="#26FFFFFF" BorderThickness="1">
                <ListBox x:Name="SuggestList" Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" FontFamily="Consolas" FontSize="12" Padding="3,2" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
            <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="#E0746A" Margin="0,8,0,0" TextWrapping="Wrap" Visibility="Collapsed" />
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <ui:Button x:Name="OkButton" Content="OK" Appearance="Primary" MinWidth="84" Margin="0,0,8,0" />
                <ui:Button x:Name="CancelButton" Content="Cancel" MinWidth="84" />
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>
